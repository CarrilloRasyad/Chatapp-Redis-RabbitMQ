import { createApp } from "./app.js";
import { createServer } from "http";

const main = async()=> {
    try {
        const app = createApp();
        const server = createServer(app);

        // const port = 
    } catch (error) {

    }
}

main();